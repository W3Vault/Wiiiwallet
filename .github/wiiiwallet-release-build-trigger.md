# Wiiiwallet non-debug release build

This branch triggers a signed, non-debug Android release APK build for direct beta installation.

Retry requested after making the release-signing configuration robust.
