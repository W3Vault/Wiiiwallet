<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Show the wiii logo while React Native starts on Android 11 and below. -->
        <item name="android:windowBackground">@drawable/wiiiwallet_splash</item>
        <item name="android:editTextBackground">@drawable/rn_edit_text_material</item>
        <!-- Status bar color - transparent for light theme -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <!-- Status bar text color - dark content for light theme -->
        <item name="android:windowLightStatusBar">true</item>
    </style>
    
    <!-- Widget text styles -->
    <style name="WidgetTextPrimary">
        <item name="android:textColor">@color/text_primary</item>
    </style>
    
    <style name="WidgetTextSecondary">
        <item name="android:textColor">@color/text_secondary</item>
    </style>
</resources>
